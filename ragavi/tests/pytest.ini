[pytest]
minversion = 6.0
# addopts = -ra -q
addopts = -v -p no:warnings
testpaths =
    .

# https://stackoverflow.com/questions/54722420/suppress-pytest-warnings-for-other-peoples-code-only
filterwarnings = 
    ignore:::!ragavi: